enum AppStrings {
  // General strings
  emptyText(''),

  // API query params
  page('page'),

  // Placeholder or unknown text
  unknownText('???'),
  required('*'),
  ;

  final String text;

  const AppStrings(this.text);
}
